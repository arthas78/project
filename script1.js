const numberOfFilms = prompt(`How many movies have you watch already?,""`);
let personalMovieDB = {
  count: numberOfFilms,
  movies: {
    lastMovie: rate,
  },
  actors: {},
  genres: [],
  privat: false,
};

let lastMovie;
let rate;

a = prompt(`What was the last movie you watched?`, `${lastMovie}`);
prompt(`Рow much would you rate it?`, `${rate}`);

personalMovieDB.movies[a] = b;
